# Dashboard implementation checkpoint — September 4, 2026

This independent implementation repository remains at detached snapshot `24039b5fc93ccbfa80bd07d59162a7987f9182dd`; that is its recovery point, not the current remote baseline. On September 4, GitHub `main` was freshly fetched into `refs/audit/origin-main-20260904`, resolving to `2cb5bf0b5207346a08d2207515d972e6f8f048c7` (committed September 1, 2026 at 10:19:48 -05:00; checked September 4 at 16:13:02 UTC). The page inventory now uses that current-main tree. The [current-main reconciliation](./current-main-reconciliation.md) covers every newer path since the original `11ce67643055a9afbedde24b63a4945f779ae691` checkout and identifies source-backed port corrections. That is not a claim of complete end-to-end behavior verification. The original checkout, saved snapshot and implementation changes are preserved. This note supersedes the historical migration handoff, including its old API-owned bot requirements.

## Corrections

- The frontend remains Vite, React, TanStack Router/Query, TypeScript 7.0.2 and Effect 4.0.0-rc.112 on its own Cloudflare Worker. Existing components/styles remain; no redesign or new general API proxy was added.
- Restored the original public `GET /api/tenor-media?url=…` on that Worker. Its helper is byte-identical to the original source: supported Tenor view URLs, GIF `307` redirect, cache headers, and `400`/`404`/`405`/`502` failures remain. The reminder image is again 640×360 without an authenticated central POST request. Baseline upstream redirect following and lack of a dedicated response-size/timeout limit were not silently changed.
- Restored the original giveaways loading component using the equivalent client translation hook, and connected it plus the existing Dashboard loading component to TanStack pending routes. The previous placeholders now remain during code loading.
- Updated the independently derived page inventory to freshly fetched GitHub `main`: 47 pages in that source, 46 retained in the implementation, creator review explicitly retired by the user. Connect is also explicitly retired and already absent from current main. Neither retired feature is a remaining gap or a reason to restore old UI.
- Restored original ticket identities: panels are keyed, refreshed and deleted by their names, and buttons by `custom_id`, rather than new database IDs absent from the original API. Ticket-message editing keeps the original 25-row add limit and 100/2,000-character input limits, but can save all previously loaded rows without imposing new server rules, trimming names or dropping invalid rows. A blank name still shows the original validation error. The roster fixture now checks only retained response fields, not deferred bot configuration.
- Reconciled current main's login/callback/settings changes and removed the obsolete consent-only English fallback. Locale loading is now directly typed against the complete English catalog, with the existing strict 30-locale validator.
- Corrected two typed-transport regressions: one failed Hall-limit request no longer discards the other successful result, and a failed reminder-list refresh no longer makes an already successful save/clone appear to have failed. The latter has before/after failing regression evidence and never retries the mutation.
- Shared packages use matching provisional local archives, lockfile integrity and a checkout-local clean install. The original intended registry dependencies were both `0.1.0-rc.0`; registry publication and final release installation remain separate work.
- Pinned Wrangler to 4.128.0, matching the API validation tool. The previously locked 4.125.0 runtime refused the configured September 3 compatibility date; the date and production bindings were not weakened to accommodate it.

## Validation

Using Node 26.8.1 and npm 12.0.1 against the refreshed provisional package pair: clean `npm ci --ignore-scripts --no-audit --no-fund` installed 501 packages. All **761 tests in 110 files, with no TODOs**, frontend plus both Worker TypeScript checks, lint, all 30 locale catalogs, production build, generated public HTML, bundle budgets and `git diff --check` passed. An independent `git ls-tree` comparison confirmed that the inventory matches all 47 page source files in the fetched main commit. The final production build contains 3,869 client modules and five Worker modules; the frontend Worker bundle is 3.17 kB (1.33 kB gzip). A final Wrangler 4.128.0 dry run accepted 250 static files and the assets-only binding without uploading anything. The regenerated manifest has 139 client methods and 77 direct callers; caller coverage does not prove every API behavior.

The two local archives are locked by their actual SHA-512 integrity. Their SHA-256 hashes are contracts `9d7ba741673dd37f4024cd620f3342eb0e6b5d43de5994909f1ba53d0317848b` and client `6efb92517bea793d891ca255d0aaf1602cd06e058652d6d3e2523e67ab689035`. After regenerating only the shared-package lock entries and a checkout-local clean install from a fresh cache, all 144 contract files and six client files matched the archive bytes, including the licenses. All validation above was rerun against this pair, which includes the API's corrected Tracking responses, canonical Bot/Dashboard descriptors and source-backed strike defaults. The pair remains provisional and unpublished.

The earlier code-only HTTP checks passed: `/login`, `/dashboard/roles` and `/fr/privacy` returned HTML with status 200, and an invalid Tenor URL returned JSON with status 400. Local Wrangler used `--local-upstream dash.clashk.ing` to select the Dashboard host; its default first configured marketing host instead produces the expected 308 Dashboard-domain redirect. Redirects were not followed to production. The local Worker had only the local assets binding and was stopped after validation. These HTTP checks predate the final package refresh; the final evidence is the fresh install, full tests/types/build and dry run above, not a repeated live HTTP session.

npm blocked lifecycle scripts for esbuild, fsevents, msgpackr-extract and the two workerd versions in the earlier install; the final install explicitly used `--ignore-scripts`. Installed platform binaries still permitted validation; no blocked script permission was overridden. The test suite retains existing mocked-error/React `act` warnings, and the build retains its large-chunk advisory while passing the checked bundle budgets. One intermediate build reported a sandbox-denied home log path; the final build and dry run use an explicit temporary log directory and passed without that error.

## Remaining gaps

1. Complete coordinated auth/API behavior checks before any future cutover. Current-main changes, including login/callback/settings and their related removals, have now been source-reconciled; page presence, static HTTP checks and a green build still do not prove all workflows or live cross-origin login. The current provisional pair has been cleanly installed and checked; any subsequent shared-package change requires another coordinated refresh.
2. Visual equivalence is unverified because browser/screenshot/app visual tests are prohibited. None were run.
3. Registry publication, release-package installation and deployment are separate future work. No publication, commit, push, deployment, production migration/resource change, traffic change or native release was performed. API-owned bot orchestration is deferred, not a Dashboard release requirement.

## Creator retirement and current-main evidence

The user explicitly confirmed creator review is retired. Current main still has the stale creator-application [list request at line 45](https://github.com/ClashKingInc/ClashKingDashboard/blob/2cb5bf0b5207346a08d2207515d972e6f8f048c7/app/admin/creators/page.tsx#L45), [review request at line 65](https://github.com/ClashKingInc/ClashKingDashboard/blob/2cb5bf0b5207346a08d2207515d972e6f8f048c7/app/admin/creators/page.tsx#L65), and [navigation link at line 107](https://github.com/ClashKingInc/ClashKingDashboard/blob/2cb5bf0b5207346a08d2207515d972e6f8f048c7/components/servers-header.tsx#L107). These were verified with `git grep` against the exact freshly fetched SHA above, not the saved implementation snapshot. Source presence does not override the user's retirement decision or prove a deployed backend; no UI or endpoint was restored. This feature is separate from connected-app consent, which current main already removed in commit `2cb5bf0` (PR #400).

The design skill guided reuse of the current source because its two older App reference files are missing. Cloudflare guidance kept local checks separate from deployment. The restored loading boundary uses [TanStack's documented pendingComponent behavior](https://tanstack.com/router/latest/docs/guide/data-loading).
# September 5 review fixes

Marketing application links and direct Discord-login buttons now navigate to `dash.clashk.ing` before creating the PKCE verifier/state. A successful refresh from an earlier session generation cannot restore a logged-out session, including a refresh waiting for a browser lock or storage lease. Generated and client-created metadata now share cleanup, and route changes update locale selection.

Public metadata is derived from the authoritative locale JSON during Vite builds through a small virtual module. Complete French and Dutch Dashboard translations no longer enter the initial dependency graph. The measured initial JavaScript total fell from 343,021 to 267,098 gzip bytes in the isolated fix comparison (267,196 bytes in the final rc.1 build); an aggregate 300 KiB initial budget now supplements per-chunk budgets. This is a byte-size improvement, not a measured production load-time claim.

Final verification against the matching, unpublished `0.1.0-rc.2` shared pair passed 767 tests in 112 files, lint, all three TypeScript checks, the production build/public-shell/bundle checks and `git diff --check`. The pair is now vendored inside this repository; sibling implementation checkouts are no longer needed to install it. Guild detail typing follows the canonical full `GuildDetails` response, while the list remains `GuildInfo[]`. The API manifest is regenerated by its normal script (139 endpoints, 75 direct callers). No deployment or visual tests were performed.
