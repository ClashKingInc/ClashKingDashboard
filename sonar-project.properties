# SonarCloud configuration
# https://sonarcloud.io — connect via GitHub OAuth, create project, copy the key below

# projectKey and organization are injected via GitHub secrets in ci.yml
# (SONAR_PROJECT_KEY, SONAR_ORGANIZATION)

# Source files
sonar.sources=app,components,lib,messages
sonar.exclusions=\
  node_modules/**,\
  .next/**,\
  out/**,\
  public/**,\
  **/*.test.ts,\
  **/*.test.tsx,\
  **/*.spec.ts,\
  **/*.spec.tsx

# Encoding
sonar.sourceEncoding=UTF-8

# TypeScript
sonar.javascript.lcov.reportPaths=coverage/lcov.info

# Coverage exclusions
# lib/api/clients/ are thin wrappers delegating entirely to BaseApiClient.
# No branching logic to unit-test -- integration tests are the right tool here.
sonar.coverage.exclusions=\
  lib/api/clients/**,\
  lib/api/index.ts
